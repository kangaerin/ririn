변수(variable) : 프로그램을 실행하는 동안 값이 여러 번 달라질 수 있는 데이터
한 함수안에서만 사용할 수 있는 변수 : local variable
스크립트 소스 전체에서 사용할 수 있는 변수 : global variable
선언 : 
(1) var <- 값 재할당, 재선언 가능.
+ var를 사용한 변수는 선언하기 전에 사용하면 오류를 발생시킬 수 있다.
프로그램 길이가 길어지다보면 실수로 변수를 재선언하거나 값을 재할당해버리는 경우가 생김.
(2) let <- 값 재할당은 가능하지만 변수를 재선언하는 것은 불가능

상수(constant) : 값을 한 번 지정하면 바뀌지 않는 데이터 
선언 : const <- 재선언, 재할당 할 수 없음.

변수때문에 생기는 오류를 줄이려면 let, const를 사용하는 것이 좋고,
값이 자주 바뀌는 변수라면 let, 재할당이 없는 변수라면 const를 사용한다.

비교 연산자(comparision operaiors)
== : 피연산자가 서로 같은 경우 True
=== : 피연산자도 같고 자료형도 같으면 True
!= : 피연산자가 서로 같지 않으면 True
!== : 피연산자가 같지 않거나 자료형이 같지 않으면 True

논리 연산자(boolean operaiors)
OR : || > 피연산자중 하나가 True일 경우 True.
AND : && > 피연산자가 모두 True일 경우 True.
NOT : ! > 피연산자의 반대값을 지정

for문
for(초깃값; 조건; 증가식) {
    실행할 명령
}

함수선언
function 함수명(변수) {
    명령
}


Array 객체의 method
concat : 기존의 배열에 요소를 추가해 새로운 배열을 생섬함
every : 배열의 모든 요소가 주어진 함수에 대해 참이면 true를 반환, 아니라면 false반환
filter : 배열 요소 중에서 주어진 필터링 함수에 대해 true인 요소만 골라 새로운 배열로 생성
slice : 배열에서 특정한 부분만 잘라냄.
sort : 배열 요소를 지정한 조건에 따라 정렬함.
toString : 배열에서 지정한 부분을 문자열로 반환함.(각 요소는 ,로 구분)

Date 객체의 method
getFullYear() : 연도를 4자리 숫자로 표시
getMonth() : 0-11 사이의 숫자로 월을 표시
getDate() : 1-31 사이의 숫자로 일을 표시 
getTime() : 1970년 1월 1일 이후의 시간을 밀리 초 (1/1000)로 표시
getMilliseconds() : 0~999 사이의 숫자로 밀리초를 표시